#include <stdio.h>

int main(void)
{
	int number;
	number <= 6;

	scanf("%d", &number);

	if (number == 1 || number == 3 || number == 4) {
		printf("500");
	}
	else if (number == 2 || number ==6) {
		printf("600");
	}
	else if (number == 5) {
		printf("1000");
	}

	return 0;

}